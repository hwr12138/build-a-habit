rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // This allows any logged-in user to read and write any document.
    // Use this to test if your connection is working.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}